{% extends 'base.html.twig' %}

{% block body %}
	<div class="w-50 mx-auto" id="octopus-add-fun-fact-container">
		{{ form_start(form) }}

		{{ form_row(form.content) }}
		{{ form_row(form.friendType) }}

		<div class="d-flex justify-content-end">
			<button type="submit" class="btn btn-outline-info">Add a fun fact!</button>
		</div>

		{{ form_end(form) }}
	</div>
{% endblock %}
